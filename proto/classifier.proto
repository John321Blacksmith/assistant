syntax = "proto3";
package proto;
option go_package = "./proto";

service Classifier {
    rpc GetMainContext (BotRequest) returns (Response);
}

message BotRequest {
    string input = 1;
}

message Response {
    string output = 1;
}