[tool.poetry]
name = "bot"
version = "0.1.0"
description = "bot component for interaction with social media API"
authors = ["John321Blacksmith <kuzmichevi321@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
grpcio = "^1.83.0"
grpcio-tools = "^1.83.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
